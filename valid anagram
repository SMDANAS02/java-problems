import java.util.*;
public class val{
    public static void main(String[] args) {
        String s = "racecar";
        String t = "carrare";
        // int arr[]=new int[26];
        // for(char word1:s.toCharArray()){
        //     arr[word1-'a']++;

        // }
        // for(char word1:t.toCharArray()){
        //     arr[word1-'a']--;

        // }
        // for(int i:arr){
        //     if(i!=0){
        //         System.out.println("false");
        //         break;
        //     }

        // }
        // System.out.println("true");
        char[] ssort=s.toCharArray();
        char[] tsort=t.toCharArray();
        Arrays.sort(ssort);
        Arrays.sort(tsort);
        if(ssort.equals(tsort)){
            System.out.println("true");
        }
        else{
            System.out.println("false");
        }
    }
}
